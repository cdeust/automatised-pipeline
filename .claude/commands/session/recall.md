# Recall Session Context

Load context from a previous session.

## Instructions

1. If Cortex is available, call `cortex:recall` with the project topic and $ARGUMENTS as query.

2. Also check local cache: `tools/session-store.sh load`

3. Present the recalled context: what was decided, what was left open, what the difficulty book said.

4. If no context found, say so and suggest starting fresh.

$ARGUMENTS
