# Agent Worktree Status

Check status of running agent worktrees — active branches, last commit, uncommitted changes.

## Instructions

1. Run `tools/worktree-manager.sh list` to show all active agent worktrees.

2. If the user specified an agent name, run `tools/worktree-manager.sh status "$ARGUMENTS"` for details.

3. If no worktrees are active, say so.

$ARGUMENTS
