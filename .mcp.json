{
  "mcpServers": {
    "ai-architect": {
      "command": "bash",
      "args": [
        "-c",
        "ROOT=\"${CLAUDE_PLUGIN_ROOT:-$PWD}\" && BIN=\"$ROOT/target/release/ai-architect-mcp\" && if [ -x \"$BIN\" ]; then exec \"$BIN\"; else exec cargo run --quiet --release --manifest-path \"$ROOT/Cargo.toml\"; fi"
      ]
    }
  }
}
